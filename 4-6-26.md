# 4-5-26

1. [x] **Adding a response message in amazon thread of all the workflow message**

```
if (meta?.messageId && results.length > 0) {
yield* Effect.tryPromise({
try: async () => {
    const channel = client.channel("messaging", meta.channelId);
    await channel.sendMessage({
    text: results.join("\n"),
    parent_id: meta.messageId, 
    });
},
catch: (e) => new Error(`Failed to send reply: ${e}`),
}).pipe(
Effect.catch((e) => Effect.logError(`[Amazon] Reply failed: ${e}`)),
);
}
```
