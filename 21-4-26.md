# 20-4-2026

* Refactor the worlflowEngine Rpc 

```
import { WorkflowRpc } from "../workflow";
import { incrementWorkflow } from "./contract";

export const WorkflowRpcLive = WorkflowRpc.toLayer({
  incrementWorkflow: ({ id, value }) =>
    incrementWorkflow.execute({ id, value }),
  incrementWorkflowDiscard: ({ id, value }) =>
    incrementWorkflow.execute({ id, value }, { discard: true }),
  incrementWorkflowResume: ({ executionId }) =>
    incrementWorkflow.resume(executionId),
});
```

**Build An Extension**

> Check on this 
https://github.com/ranjitd-sys/Extension

