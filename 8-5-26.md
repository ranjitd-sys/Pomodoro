# 8-5-26

 1. [x] **Deploy Every container on k8s and make sure it is wokrinng properly**
________
 2. [x] **Added constantlly listing to message in Effect using client.on event** 
```
 channel.on("message.new", (event) => {
    console.log("Stream event received:", event.message?.text);
    Effect.runPromise(
      Queue.offer(queue, { type: "NEW_MESSAGE", message: event.message }),
    ).catch((e) => console.error("Queue offer failed:", e));
  });
  ```
--- 



