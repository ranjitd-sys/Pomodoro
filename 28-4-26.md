# 28-4-26

1. > Solving race condition of refresing on frontend the memory get wiped up spans cannot be stored here
    - solution persistant using index db
    - not valid but implemented cookie 
```
 chrome.cookies.set({
    url: "http://localhost:3000/workflow",
    name: "traceContext",
    value: JSON.stringify({
        traceId: span.traceId,
        spanId: span.spanId,
        sampled: true,
    }),
    path: "/workflow",
    secure: false,
    httpOnly: false,
    sameSite: "lax",
    });
```
