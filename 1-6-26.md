# 4-5-26

1. [x] **Removing the queue appraoch to input and contxt approach**

```
dfParsing: {
invoke: {
    src: "pdfParserActor_",
    input: ({ context }: any) => ({
    cdnLink: context.filePaths[context.currentIndex],
    }),
    onDone: [
    {
        guard: ({ context }: any) =>
        context.currentIndex < context.filePaths.length - 1,
        target: "pdfParsingNext",
        actions: assign({
        currentIndex: ({ context }: any) =>
            context.currentIndex + 1,
        }),
    },
    {
        target: "idel",
        actions: () =>
        console.log("[PdfParser] all files done"),
    },
    ],
    onError: {
    target: "idel",
    actions: ({ event }: any) =>
        console.error("[PdfParser] error", event.error),
    },
},
},
```
