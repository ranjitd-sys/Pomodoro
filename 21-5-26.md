# 21-5-2026
1. [x] **Added log notify to current amazon pod to send notifination to the user when it is completed**


<span style="color:orange; font-weight:bold;">Pending from mahesh sir to add a httpRunner </span>

2. [x] **Added Rpc to get Token and deployed**
```
import { BunHttpServer, BunRuntime } from "@effect/platform-bun"
import { Layer, pipe } from "effect"
import { RpcGroup, RpcServer, RpcSerialization } from "effect/unstable/rpc"
import { HttpRouter } from "effect/unstable/http"
import { GetstreamContracts } from "./contract"
import { GetstreamLive } from "./handerl"

BunRuntime.runMain(
  pipe(
    HttpRouter.serve(
      RpcServer.layerHttp({
        path: "/token/rpc",
        protocol: "http",
        group: RpcGroup.make().merge(GetstreamContracts)
      })
    ),
    Layer.provide(GetstreamLive),
    Layer.provide(RpcSerialization.layerNdjson), 
    Layer.provide(BunHttpServer.layer({ hostname: "0.0.0.0", port: 3000 })),
    Layer.launch
  )
)```
