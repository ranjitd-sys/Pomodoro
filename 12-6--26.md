# 11-6-26
#tasks

1. [x] **Exploring Elysia and bun http server and clould flare worker**
```
import cors from "@elysia/cors";
import Elysia, { t } from "elysia";
import { CloudflareAdapter } from "elysia/adapter/cloudflare-worker";

export default new Elysia ({
	adapter:CloudflareAdapter
})
.get("/", ()=>"It is woking")
.post("/signin", ({body})=> {
	console.log("Validate User");
	return {Vaidate:true, body};
}, {
    body:t.Object({
        name: t.String(),
        password: t.String({
            minLength:3
        })
    })
})
.compile()
.use(cors())
```
<br>

2. [x] **Exploring CloudNativePG and how it a good psql oparator** 