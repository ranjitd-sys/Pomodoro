# 11-6-26
#tasks


1. [x] **Exploring CloudNativePG and how it a good psql oparator** *|9:30 - 10:30 |*
<br>

2. [x] **Exploring Elysia and bun http server and clould flare worker** *|10:30 - 12:10|*
```
import cors from "@elysia/cors";
import Elysia, { t } from "elysia";
import { CloudflareAdapter } from "elysia/adapter/cloudflare-worker";

const Users = new Map<string,string>();

export default new Elysia ({
	adapter:CloudflareAdapter
})
.get("/", ()=>"It is woking")
.post("/signup", ({body})=> {
	console.log("Validate User");
	console.log(body)
	Users.set(body.name, body.password);
	return {Vaidate:true, body};
}, {
	body:t.Object({
            name: t.String(),
            password: t.String({
                minLength:3
            })
	})
})
.post("/signin", ({body}) => { if(!Users.has(body.name)){
    console.log("Uesr not found");
    Users.get(body.name)

} else if(Users.get(body.name)){
	console.log(Users.get(body.name))
} return body}, {
    body:t.Object({
    name:t.String(),
    password:t.String({
        minLength:3
    })
})
})
.compile()
.use(cors())
```
<br>